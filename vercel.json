{
  "version": 2,
  "description": "Fresh deployment configuration for React 18 compatibility - ready for Vercel",
  "buildCommand": "npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "outputDirectory": "build",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "functions": {
    "api/chat/*.js": {
      "maxDuration": 60
    }
  }
}
